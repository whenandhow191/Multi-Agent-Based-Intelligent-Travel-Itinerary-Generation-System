# 代码地图

> 文档版本：v3.0（C33）
> 更新日期：2026-09-13
> 适用检查点：C00～C33

## 1. 如何阅读本仓库

推荐按以下顺序理解项目：

1. `README.md`：先运行最小命令，确认本地环境可用。
2. `docs/01-需求规格说明书.md`：理解产品目标、边界和验收标准。
3. `docs/02-技术实现方案.md`：理解 1+4 Agent、自建 Harness 和供应商隔离方案。
4. `docs/03-教学式开发路线与Git交付计划.md`：了解 C00～C49 的学习顺序。
5. 本文：从路径定位到代码和配置。
6. `docs/adr/`：了解关键选择的背景、取舍和后果。

当前依赖方向为：

```text
apps/web  ──HTTP──>  apps/api  ──后续章节──>  packages/harness
                                  │                  │
                                  └──────────────>  packages/domain
                                                     │
packages/agents  ──后续通过 Harness 调用─────────────┤
packages/tools   ──后续归一化外部数据───────────────┘
packages/evals   ──测试整个系统，不被生产代码依赖
```

硬规则：领域层不得依赖应用层；Agent 不直接互调；前端不重新决定行程事实；供应商私有结构不得越过 Tool Gateway。

## 2. 根目录与工程配置

| 路径 | 职责 | 依赖/输入 | 主要使用者 |
|---|---|---|---|
| `README.md` | 项目入口、环境安装、检查与启动命令 | 三份设计文档 | 新参与者、评审者 |
| `CONTRIBUTING.md` | 提交、PR、安全数据要求 | 开发路线 | 贡献者 |
| `LICENSE` | MIT 开源许可 | 无 | 仓库使用者 |
| `.gitignore` | 排除虚拟环境、依赖、密钥、缓存和构建结果 | 本地工具生成物 | Git |
| `.gitattributes` | 统一文本为 LF，标记二进制文件 | Git 工作树 | Git、CI |
| `.editorconfig` | 统一编码、缩进和换行 | 编辑器支持 | IDE、编辑器 |
| `.env.example` | 只保存变量名和安全的本地默认值 | 配置模型 | 开发者、Compose |
| `.dockerignore` | 缩小构建上下文并隔离密钥/缓存 | Docker 构建 | API/Web Dockerfile |
| `.npmrc` | 固定项目内 pnpm store 并支持非交互安装 | pnpm | 本地、CI |
| `.pre-commit-config.yaml` | 在提交前运行格式、Lint、类型和测试 | uv、Ruff、mypy、pytest | pre-commit |
| `environment.yml` | 建立项目专用 Conda Python 3.12 引导环境 | Conda、PyPI 的 uv | 本地开发者 |
| `pyproject.toml` | Python 运行/开发依赖和 Ruff、mypy、pytest 配置 | Python 3.12+ | uv、CI、API |
| `uv.lock` | Python 依赖的跨平台精确锁定结果 | `pyproject.toml` | uv、Docker、CI |
| `package.json` | 工作区统一的格式、检查、测试和构建命令 | uv、pnpm workspace | 开发者、CI |
| `pnpm-workspace.yaml` | 声明 Web workspace，并显式允许 esbuild 安装脚本 | pnpm | 本地、CI、Docker |
| `pnpm-lock.yaml` | Node 依赖的精确锁定结果 | 根与 Web package.json | pnpm、Docker、CI |
| `compose.yaml` | 编排 PostgreSQL、API 与 Web，声明依赖和健康检查 | 两个 Dockerfile、`.env` | 本地运行时 |
| `alembic.ini`、`migrations/` | PostgreSQL Schema 版本与可升级/降级迁移 | SQLAlchemy metadata、DATABASE_URL | Alembic、Compose migrate 服务 |

## 3. 应用代码

### 3.1 FastAPI：`apps/api`

| 路径 | 职责 | 依赖/输入 | 被谁调用 |
|---|---|---|---|
| `apps/__init__.py` | 标记可部署应用命名空间 | 无 | Python 导入系统 |
| `apps/api/__init__.py` | 标记 API 包 | `apps` | 测试、uvicorn |
| `apps/api/main.py` | 创建 FastAPI、装配 CORS、提供 `GET /health` | `settings.py`、FastAPI、Pydantic | uvicorn、Web、健康检查、测试 |
| `apps/api/settings.py` | 类型化配置、dotenv 分层、生产安全校验和缓存 | pydantic-settings、环境变量 | `main.py`、`config.py`、测试 |
| `apps/api/config.py` | 实现密钥安全的 `config doctor` | `settings.py` | 开发者、诊断脚本、测试 |
| `apps/api/travelctl.py` | C32 doctor、工具发现/调用、运行检查和 Blackboard 读取 CLI | Settings、ToolGateway、PostgreSQL | 开发者、运维人员 |
| `apps/api/Dockerfile` | 用锁文件构建 Python 3.12 API 镜像 | uv、`pyproject.toml`、API/Packages | Compose |
| `apps/api/README.md` | API 层职责和边界 | 设计文档 | 开发者 |

`/health` 是不依赖数据库和外部 Provider 的进程存活检查。数据库就绪由 PostgreSQL healthcheck 负责，`migrate` 服务成功升级 Schema 后 API 才会启动。

### 3.2 React：`apps/web`

| 路径 | 职责 | 依赖/输入 | 被谁调用 |
|---|---|---|---|
| `apps/web/src/main.tsx` | 挂载 React 根组件并加载全局样式 | `App.tsx`、`styles.css` | Vite 入口 |
| `apps/web/src/App.tsx` | 展示首章占位页并探测 API health | React、`VITE_API_BASE_URL` | `main.tsx`、组件测试 |
| `apps/web/src/App.test.tsx` | 验证项目标题和 API 在线状态 | Vitest、Testing Library | `pnpm test` |
| `apps/web/src/styles.css` | 首章占位页视觉与响应式布局 | 浏览器 CSS | `main.tsx` |
| `apps/web/src/vite-env.d.ts` | 提供 Vite 环境变量类型 | Vite | TypeScript |
| `apps/web/index.html` | 浏览器 HTML 外壳 | `main.tsx` | Vite、Nginx |
| `apps/web/vite.config.ts` | 配置 React 插件和 jsdom 测试环境 | Vite、Vitest | dev/build/test |
| `apps/web/eslint.config.js` | TypeScript、Hooks、热更新规则 | ESLint 插件 | `pnpm lint` |
| `apps/web/tsconfig.json` | 聚合浏览器与构建配置 | 两个子 tsconfig | TypeScript |
| `apps/web/tsconfig.app.json` | 浏览器源码严格类型配置 | React/Vite 类型 | TypeScript |
| `apps/web/tsconfig.node.json` | Vite 配置文件类型检查 | Node 类型 | TypeScript |
| `apps/web/package.json` | Web 依赖与 dev/build/test 命令 | pnpm lock | 根脚本、Docker |
| `apps/web/nginx.conf` | SPA fallback 和容器 health 路径 | Nginx | Web 镜像 |
| `apps/web/Dockerfile` | 构建静态资源并交给 Nginx | Node、pnpm、Nginx | Compose |
| `apps/web/README.md` | Web 层职责和开发命令 | 设计文档 | 开发者 |

## 4. 可复用包

领域契约与 Harness 内核已经形成可导入边界；后续业务 Agent 只能通过这些公共接口工作。

| 路径 | 当前职责 | 未来主要消费者 |
|---|---|---|
| `packages/__init__.py` | 标记共享包命名空间 | 所有 Python 应用 |
| `packages/domain/common.py` | 不可变、禁止额外字段的 Pydantic 基类与公共标量 | 全部领域模型 |
| `packages/domain/trip_request.py` | TripRequest、预算、硬约束、软偏好与澄清问题 | API、Coordinator、Planner |
| `packages/domain/messaging.py` | Task/Event/ArtifactRef/Claim/Evidence 与状态枚举 | Harness、Agents、持久化层 |
| `packages/domain/travel.py` | POI、路线、天气、住宿、交通、费用和显式坐标系模型 | Tool Adapters、A1/A2、Planner |
| `packages/domain/itinerary.py` | PlanCandidates/Review/FinalPlanBundle 与对比、修复契约 | A3、A4、Aggregator、API/Web |
| `packages/domain/validators.py` | 预算、时间窗、重叠、开放时间、坐标系和跨对象引用的确定性校验 | Critic、Aggregator、API、Evals |
| `packages/domain/__init__.py` | 领域公共 API，集中导出稳定契约 | API、Harness、Agents、Tools |
| `packages/harness/model_gateway.py` | Provider-neutral 模型消息、工具、策略、Usage、请求/响应和 Gateway 协议 | Agents、模型适配器、Evals |
| `packages/harness/tool_gateway.py` | 工具注册、JSON Schema、allowlist、输入/输出校验和唯一执行入口 | Agents、Tool Adapters、CLI/MCP/API |
| `packages/harness/agent.py` | Agent 上下文、有限模型/工具循环、步骤/调用/超时限制、结构化输出与错误分类 | 所有业务 Agent、Scheduler |
| `packages/harness/scheduler.py` | DAG 合法性、就绪判断、并发执行、协作取消、超时与有限重试 | Worker、Coordinator、API |
| `packages/harness/persistence/schema.py` | runs/tasks/dependencies/events 的 SQLAlchemy Core 权威定义 | Alembic、Repository |
| `packages/harness/persistence/repository.py` | Repository 协议、内存契约实现、PostgreSQL 原子租约与幂等事件 | Worker、Scheduler、测试 |
| `packages/harness/recovery.py` | heartbeat、检查点恢复、过期租约回收和 Outbox 发布循环 | Worker、后台发布器 |
| `packages/harness/blackboard.py` | C23 版本化 Artifact、内容哈希、lineage、内存/PostgreSQL 后端和乐观并发保护 | Workflow、CLI |
| `packages/harness/blackboard_cli.py` | C23 按 run/task/type/version 读写 PostgreSQL Artifact 的最小诊断 CLI | 开发者 |
| `packages/harness/` | 自建状态机、DAG、调度、策略和恢复的公共 API | API、Worker、Agents |
| `packages/agents/coordinator.py` | C18 固定白名单任务图、阻塞澄清和调度预算决策；不执行任务或生成旅行事实 | Harness、API |
| `packages/agents/destination_intelligence.py` | C19 A1 工具白名单、目的地结构化输出及跨 Place/Weather/Claim/Evidence 引用闭包 | Harness、A3 |
| `packages/agents/mobility_lodging.py` | C20 A2 城际交通/住宿/定向路线契约，以及未知价格、供应商失败和人工确认降级 | Harness、A3 |
| `packages/agents/itinerary_planning.py` | C21 A3 工具白名单、OR-Tools 候选选择、路线/开放时间排程及确定性复验 | Harness、A4 |
| `packages/agents/critic_risk.py` | C22 A4 确定性复验、证据/矛盾/折返风险审校、评分及结构化返工路由 | Harness、Coordinator |
| `packages/agents/` | Coordinator 与四个专家 Agent 的公共导出 | Harness |
| `packages/workflow/orchestrator.py` | C24 A1/A2 并行 fan-out、Harness Route Matrix、A3/A4 fan-in、Artifact 发布与公开 trace | API、Worker |
| `packages/workflow/repair.py` | C25 PatchRequest 目标解析、依赖失效闭包、两轮上限、版本化局部返工和澄清恢复 | Coordinator、Worker |
| `packages/workflow/final_aggregator.py` | C26 最终复验、多方案比较、Evidence 新鲜度汇总及同源 JSON/Markdown 渲染 | API、导出层 |
| `packages/workflow/` | 1+4 工作流公共导出与边界说明 | API、Worker |
| `packages/tools/http_provider.py` | C27 超时、重试、限流、熔断、缓存、审计和字段级存储策略 | httpx、StoragePolicy | 所有 HTTP Provider Adapter |
| `packages/tools/amap.py` | C28 高德地理编码/POI 归一化及无 Key Fixture 降级 | HTTP Provider、领域模型 | A1、CLI |
| `packages/tools/routes.py` | C29 高德路线、受限 Top-K 矩阵、polyline 和坐标转换 | HTTP Provider、领域模型 | Harness、A2、Web |
| `packages/tools/weather.py` | C30 Open-Meteo 精确预报范围、气候提示和失败降级 | HTTP Provider、领域模型 | A1 |
| `packages/tools/travel_stubs.py` | C31 航班/酒店参考接口与 12306 官方跳转，不冒充实时库存 | 领域模型 | A2、CLI |
| `packages/tools/registry.py` | C32 REST/Fixture 工具的统一组合根 | Settings、各 Adapter | travelctl、后续 API |
| `packages/tools/mcp_adapter.py` | C33 stdio/Streamable HTTP MCP、发现快照、名称映射、超时与权限 | JSON-RPC、httpx、ToolGateway | 可选 MCP 集成 |
| `packages/tools/` | 供应商归一化、工具注册及 REST/CLI/MCP/Fixture 适配边界 | Harness、Agents |
| `packages/evals/fixtures.py` | 固定时间、ID、金额和引用关系的完整合成场景工厂 | 离线单元测试、演示 |
| `packages/evals/scripted_model.py` | 顺序返回预校验响应的无网络模型替身 | Harness/Agent 单元测试 |
| `packages/evals/` | 评测公共 API、数据集、运行器和质量/成本指标 | CI、评测命令 |

每个目录的 `__init__.py` 使空包可导入，`README.md` 说明职责与禁止跨越的边界。

## 5. 测试与交付配置

| 路径 | 验证目标 | 运行入口 |
|---|---|---|
| `tests/test_package_imports.py` | C01 中所有空包可导入 | pytest |
| `tests/test_health.py` | `/health` 的 HTTP 状态和响应契约 | pytest + httpx ASGI transport |
| `tests/test_settings.py` | 配置优先级、生产保护、密钥不泄漏 | pytest |
| `tests/test_trip_request.py` | C06 示例 JSON 往返、日期冲突和额外字段拒绝 | pytest |
| `tests/test_messaging.py` | C07 状态枚举、必填字段、证据和时区约束 | pytest |
| `tests/test_travel_models.py` | C08 跨供应商归一化、未知费用和坐标边界 | pytest |
| `tests/test_itinerary_contracts.py` | C09 完整示例、时间顺序、审校与比较覆盖 | pytest |
| `tests/test_synthetic_fixtures.py` | C10 合成场景字节级确定性、无密钥 ScriptedModel 与耗尽行为 | pytest |
| `tests/test_validators.py` | C11 合法方案与超预算、闭馆、重叠、越界、坐标/引用错误 | pytest |
| `tests/test_model_gateway.py` | C12 统一模型接口、Usage、工具调用与歧义响应拒绝 | pytest |
| `tests/test_tool_gateway.py` | C13 工具发现、Fixture 执行、越权/非法参数与重复注册拒绝 | pytest |
| `tests/test_agent_lifecycle.py` | C14 Echo Agent 完成、工具循环、Schema 失败、步骤上限与超时 | pytest |
| `tests/test_scheduler.py` | C15 DAG 缺失/环检测、真实并发、依赖顺序、取消、超时与有限重试 | pytest |
| `tests/test_repository.py` | C16 双 Worker 原子租约、依赖阻塞、事件幂等及 PostgreSQL SQL 形状 | pytest |
| `tests/test_recovery.py` | C17 heartbeat、进程重启续跑、最新检查点与并发 Outbox 发布 | pytest |
| `tests/test_coordinator.py` | C18 固定 DAG、Harness 路线矩阵节点、阻塞澄清与预算上限 | pytest |
| `tests/test_destination_intelligence.py` | C19 ScriptedModel + Fixture Tool、A1 Schema 与无来源事实拒绝 | pytest |
| `tests/test_mobility_lodging.py` | C20 ScriptedModel + Fixture Tool、非实时库存与人工确认降级 | pytest |
| `tests/test_itinerary_planning_agent.py` | C21 三种离线候选、无重叠/引用校验与硬预算明确失败 | pytest |
| `tests/test_critic_risk_agent.py` | C22 合法方案通过及闭馆、超预算、折返、缺证据的检测与返工路由 | pytest |
| `tests/test_postgres_repository.py` | CI PostgreSQL 16 上真实执行原子租约与事件幂等契约 | pytest + GitHub service |
| `tests/test_blackboard.py` | C23 内存 Blackboard 精确/最新版本、lineage、哈希防篡改、并发冲突和 CLI 参数 | pytest |
| `tests/test_postgres_blackboard.py` | C23 PostgreSQL 真实并发版本冲突与读取 | pytest + GitHub service |
| `tests/test_one_plus_four_workflow.py` | C24 真实 A1/A2 并发、结构化 ArtifactRef 交接、依赖顺序和完整 lineage | pytest |
| `tests/test_targeted_repair.py` | C25 酒店局部返工、未受影响 A1 复用、版本 lineage、两轮上限和人工澄清恢复 | pytest |
| `tests/test_final_aggregator.py` | C26 三方案比较、确定性 JSON/Markdown、无润色模型输出及 fail-closed 复验 | pytest |
| `tests/test_http_provider.py` | C27 200/429/500/超时、缓存、熔断与存储白名单 | pytest + httpx MockTransport |
| `tests/test_amap.py` | C28 无 Key Fixture 与高德响应归一化、密钥不进入输出 | pytest |
| `tests/test_routes.py` | C29 路线矩阵上限、polyline 和 WGS84/BD09 转 GCJ-02 | pytest |
| `tests/test_weather.py` | C30 预报归一化、远期提示和失败 Fixture 降级 | pytest |
| `tests/test_travel_stubs.py` | C31 非实时航班/酒店和 12306 官方跳转契约 | pytest |
| `tests/test_travelctl.py` | C32 工具发现、ToolGateway 调用和非法 JSON 拒绝 | pytest |
| `tests/test_mcp_adapter.py` | C33 allowlist、名称映射、远端 Schema 和 HTTP/SSE 会话 | pytest + httpx MockTransport |
| `tests/README.md` | 测试目录当前用途和未来分层 | 开发者 |
| `.github/workflows/ci.yml` | 启动 PostgreSQL 16、升级 migration，再重放格式、Lint、类型、测试和构建 | GitHub Actions |
| `.github/ISSUE_TEMPLATE/` | 结构化 Bug/功能反馈并提醒脱敏 | GitHub Issues |
| `.github/pull_request_template.md` | PR 的检查、文档和数据安全清单 | GitHub Pull Request |

统一入口 `pnpm check` 按顺序运行：Python/前端格式检查 → Ruff/ESLint → mypy/TypeScript → pytest/Vitest → Vite 生产构建。任何一步失败都会让本地命令和 CI 失败。

## 6. 文档与 ADR

| 路径 | 内容 |
|---|---|
| `docs/01-需求规格说明书.md` | 产品需求、Agent 体系、功能与非功能验收 |
| `docs/02-技术实现方案.md` | 技术栈、Harness、Agent、工具、存储、安全与测试设计 |
| `docs/03-教学式开发路线与Git交付计划.md` | 9 章 50 个检查点与 Git 交付策略 |
| `docs/04-代码地图.md` | 文件职责、依赖方向和调用关系（本文） |
| `docs/05-第一章实现说明.md` | C00～C05 的实际操作、验证和复现方法 |
| `docs/06-第二章实现说明.md` | C06～C11 的领域建模、离线 Fixture、确定性校验和复现方法 |
| `docs/07-第三章实现说明.md` | C12～C17 的 Gateway、Agent、DAG、持久化与恢复复现方法 |
| `docs/08-第四章实现说明.md` | C18～C22 的 Coordinator、四个 Agent 文件、逻辑、权限、验收与复现方法 |
| `docs/09-第五章实现说明.md` | C23～C26 的 Blackboard、fan-out/fan-in、局部返工和最终聚合复现方法 |
| `docs/10-第六章实现说明.md` | C27～C33 的真实工具、降级边界、CLI、MCP 与密钥安全复现方法 |
| `docs/adr/README.md` | ADR 编号、状态和修改流程 |
| `docs/adr/0000-模板.md` | 新架构决策的复制模板 |
| `docs/adr/0001-工程基础与本地运行时.md` | 第一章关键工程选择与后果 |
| `docs/adr/0002-领域契约与确定性校验.md` | 供应商中立契约、证据引用和纯函数硬校验决策 |
| `docs/adr/0003-Harness内核与持久恢复.md` | 自建有限循环、PostgreSQL 队列、租约、检查点和 Outbox 决策 |
| `docs/adr/0004-1+4-Agent职责与确定性边界.md` | Coordinator/Harness/A1～A4 的职责、Artifact 与确定性边界 |
| `docs/adr/0005-共享黑板与可验证工作流.md` | Artifact 版本、lineage、失效传播和无模型最终渲染决策 |
| `examples/trip_request.beijing.json` | 可解析的纯合成 TripRequest 示例 |
| `examples/final_plan_bundle.beijing.json` | 可生成并重新校验的完整 FinalPlanBundle 示例 |
| `fixtures/synthetic/scripted_turns.json` | 无网络 ScriptedModel 按顺序消费的纯合成响应 |

## 7. 代码地图维护规则

- 每个检查点新增、删除或改变职责的源码文件，都必须同步更新本文。
- 只写稳定职责和依赖方向，不复制实现代码。
- 新的跨模块依赖必须符合本页依赖方向；例外要先写 ADR。
- 文件移动后同时更新 README、测试引用、Compose/CI 路径和本文。
